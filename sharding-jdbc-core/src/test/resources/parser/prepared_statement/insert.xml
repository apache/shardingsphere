<?xml version="1.0" encoding="UTF-8"?>
<asserts>
    <assert id="assertInsertWithParameter" sql="insert into `order`('order_id','user_id','status') values(?,?,'RUNNING')">
        <tables>
            <table name="order" />
        </tables>
        <conditions>
            <condition column-name="order_id" table-name="order" operator="EQUAL">
                <value index="0"/>
            </condition>
            <condition column-name="user_id" table-name="order" operator="EQUAL">
                <value index="1"/>
            </condition>
            <condition column-name="state" table-name="order" operator="EQUAL">
                <value literal="RUNNING" type="java.lang.String" />
            </condition>
        </conditions>
    </assert>
    <assert id="assertInsertWithParameterAndGenerateKeyColumnIsShardingColumn" sql="insert into `order`('state') values(?)">
        <tables>
            <table name="order" />
        </tables>
        <conditions>
            <condition column-name="state" table-name="order" operator="EQUAL">
                <value index="0"/>
            </condition>
        </conditions>
    </assert>
    <assert id="assertInsertWithParameterAndGenerateKeyColumn" sql="insert into `payment`('state') values(?)">
        <tables>
            <table name="payment" />
        </tables>
        <conditions>
            <condition column-name="state" table-name="payment" operator="EQUAL">
                <value index="0" />
            </condition>
        </conditions>
    </assert>
</asserts>
